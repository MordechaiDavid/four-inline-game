
/*
<div id={"container"}>
    {this.state.gameTable.map( (row, rowIndex)=>{
        return(
            row.map((column, columnIndex)=> {

                    return (
                        <div onClick={this.cellClicked(rowIndex, columnIndex)} class={"item-" + this.state.gameTable[rowIndex][columnIndex]}></div>
                    )
                }

            )
        )
    }
    }
</div>
)
}

 */